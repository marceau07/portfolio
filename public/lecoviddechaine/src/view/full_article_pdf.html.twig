<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1"> <!-- Ensures optimal rendering on mobile devices. -->
        <meta http-equiv="X-UA-Compatible" content="IE=edge" /> <!-- Optimal Internet Explorer compatibility -->
        <style>
            #top-article {
                padding: .2em;
                border-radius: .2em;
                display: inline-flex;
                font-weight: bold;
                align-items: center;
            }
            #title {
                text-align: center;
            }
            table, td {
                border: 1px black solid;
            }
            table {
                border-spacing: 0;
            }
        </style>
    </head>
    <body>
        <div id='top-article'>

            <h1><img src='../../img/logoLeCovidDechaine.png' width='100' height='75' id='logo'/>&nbsp;<span id='title'>{{ form.article.titleArticle }}</span></h1>
        </div> 
        <div id='body-article'>
            {% if form.article.imageArticle is not null %}
                <img id="imageArticleDisp" style="max-height: 200px;" src="{% if form.article.idCategory != 5 %}../web/img/articles/{% endif %}{{ form.article.imageArticle }}" class='float-left'/>
            {% else %}
                <img id="imageArticleDisp" style="max-height: 200px;" src="../web/img/articles/{{ form.article.labelCategory|lower|replace({'à':'a'})|replace({'é':'e'}) }}.png" width="250" height="200"/>
            {% endif %}
            {{ form.article.contentArticle|striptags('<b>,<u>,<i>,<p>,<br>,<ol>,<li>,<ul>,<table>,<td>,<tr>,<div>,<a>,<img>')|raw }}
        </div>
        <div id='footer-article'>
            <small>Source:&nbsp;<a href='{{ form.article.sourceArticle }}'>{{ form.article.sourceArticle }}</a></small>
        </div>
    </body>
</html>