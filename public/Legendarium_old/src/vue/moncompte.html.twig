{% extends "base.html.twig" %}
{% block title %} {{ parent() }} - Mon Compte {% endblock %}
{% block contenu %}
    <center><h1>Mon compte</h1></center>
    <div class="panel panel-default">
    <div class="table-responsive">
        <table class="table table-hover table-striped">
        <tr>
            <th>Email</th>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Pseudonyme</th>
            <th>Téléphone</th>
            <th>Rôle</th>
            <th>Photo de l'utilisateur</th>
            <th></th>
            <th></th>
        </tr>
        
        <tr>
            <td>{{session.login}}</td>
            <td>{{session.nom}}</td>
            <td>{{session.prenom}}</td>
            <td>{{session.pseudo}}</td>
            <td>{{session.telephone}}</td>
            <td>{{session.libelleRole}}</td>
            <td>
                <img src="images/utilisateurs/{{session.photo}}" alt= "Photo de l'utilisateur" width="150" height="150">
            </td>
            <td> 
                <div class="btn-group btn-group-lg"><a href="index.php?page=modifutilisateur&email={{u.email}}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                </div>
            </td>
            <td>
                <div class="btn-group btn-group-lg"><a href="#"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a></div>
            </td>
        </tr>
        </table>
        
    </div>
        
</div>
    <a href="index.php?page=mesdonnees"><p>Récupérer mes données !</p></a>
{% endblock %}