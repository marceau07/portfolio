{% extends 'base.html.twig' %}
{% block title %}
{% endblock %}
{% block contenu %}
   
<div class="container">
  <h1> Ajouter un mode de paiement !</h1>

    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_label(form.abonnement, 'Abonnement:', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}        
        <div class="col-sm-10"> 
            {{ form_errors(form.abonnement) }}
            {{ form_widget(form.abonnement) }}
            {{ form_help(form.abonnement) }} 
        </div>
    </div>
    <div class="form-group">
        {{ form_label(form.date_deb, "Début de l'abonnement:", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}        
        <div class="col-sm-10"> 
            {{ form_errors(form.date_deb) }}
            {{ form_widget(form.date_deb) }}
            {{ form_help(form.date_deb) }} 
        </div>
    </div>
    {{ form_widget(form.save) }} 
</div>
{{ form_end(form) }}

{% endblock %}